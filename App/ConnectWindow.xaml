<Window x:Class="Remotier.ConnectWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialIcons="clr-namespace:Material.Icons.WPF;assembly=Material.Icons.WPF"
        Title="Connect to Host" Height="600" Width="500"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundBrush}">
    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Margin="0,0,0,25">
             <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                 <materialIcons:MaterialIcon Kind="LinkVariant" Width="24" Height="24" Foreground="{StaticResource AccentBrush}" Margin="0,0,10,0"/>
                 <TextBlock Text="CONNECT TO PC" FontSize="20" FontWeight="Bold" Foreground="{StaticResource AccentBrush}"/>
             </StackPanel>
             <Button HorizontalAlignment="Right" VerticalAlignment="Center" Background="Transparent" BorderThickness="0" Click="Settings_Click" Cursor="Hand">
                <materialIcons:MaterialIcon Kind="Cog" Width="20" Height="20" Foreground="{StaticResource AccentBrush}"/>
            </Button>
        </Grid>
        
        <!-- Input Section -->
        <StackPanel Grid.Row="1">
            <TextBlock Text="Host IP Address" Foreground="#AAAAAA" Margin="2,0,0,5"/>
            <Border Background="{StaticResource SurfaceBrush}" CornerRadius="5" Padding="5" Margin="0,0,0,10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <materialIcons:MaterialIcon Kind="IpNetwork" Width="20" Height="20" Foreground="Gray" Margin="5,0,10,0" VerticalAlignment="Center"/>
                    <TextBox Grid.Column="1" x:Name="IpInput" Text="127.0.0.1" Background="Transparent" BorderThickness="0" Foreground="White" VerticalContentAlignment="Center" FontSize="14"/>
                </Grid>
            </Border>

            <Grid Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Margin="0,0,5,0">
                    <TextBlock Text="Account Name (Opt)" Foreground="#AAAAAA" Margin="2,0,0,5" FontSize="11"/>
                    <Border Background="{StaticResource SurfaceBrush}" CornerRadius="5" Padding="5">
                         <TextBox x:Name="AccountNameInput" Background="Transparent" BorderThickness="0" Foreground="White" VerticalContentAlignment="Center"/>
                    </Border>
                </StackPanel>
                <StackPanel Grid.Column="1" Margin="5,0,0,0">
                    <TextBlock Text="Password" Foreground="#AAAAAA" Margin="2,0,0,5" FontSize="11"/>
                    <Border Background="{StaticResource SurfaceBrush}" CornerRadius="5" Padding="5">
                         <Grid>
                             <Grid.ColumnDefinitions>
                                 <ColumnDefinition Width="*"/>
                                 <ColumnDefinition Width="Auto"/>
                             </Grid.ColumnDefinitions>
                             <PasswordBox x:Name="PasswordInput" Background="Transparent" BorderThickness="0" Foreground="White" VerticalContentAlignment="Center"/>
                             <TextBox x:Name="PasswordVisibleInput" Background="Transparent" BorderThickness="0" Foreground="White" VerticalContentAlignment="Center" Visibility="Collapsed"/>
                             <ToggleButton x:Name="ShowPasswordToggle" Grid.Column="1" Width="20" Height="20" Background="Transparent" BorderThickness="0" Checked="ShowPassword_Checked" Unchecked="ShowPassword_Unchecked" Cursor="Hand">
                                 <materialIcons:MaterialIcon Kind="Eye" Width="14" Height="14" Foreground="{StaticResource AccentBrush}"/>
                             </ToggleButton>
                         </Grid>
                    </Border>
                    <!-- Make Password Box Height Match -->
                </StackPanel>
            </Grid>

            <Button Content="Connect" Click="Connect_Click" Background="{StaticResource AccentBrush}" Foreground="White" BorderThickness="0" Padding="15,8" HorizontalAlignment="Stretch">
                 <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="5">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="{TemplateBinding Padding}">
                                <materialIcons:MaterialIcon Kind="Check" Width="16" Height="16" Margin="0,0,5,0"/>
                                <TextBlock Text="{TemplateBinding Content}" FontWeight="Bold"/>
                            </StackPanel>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.9"/>
                                <Setter Property="Cursor" Value="Hand"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </StackPanel>

        <!-- Discovery Tabs -->
        <TabControl Grid.Row="2" Margin="0,20,0,0" Background="Transparent" BorderThickness="0">
            <TabControl.Resources>
                <Style TargetType="TabItem">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Foreground" Value="Gray"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="TabItem">
                                <Border Background="{TemplateBinding Background}" Padding="10" BorderThickness="0,0,0,2" BorderBrush="{TemplateBinding BorderBrush}">
                                    <StackPanel Orientation="Horizontal">
                                        <ContentPresenter ContentSource="Header"/>
                                    </StackPanel>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Foreground" Value="{StaticResource AccentBrush}"/>
                                        <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </TabControl.Resources>

            <TabItem>
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal">
                         <materialIcons:MaterialIcon Kind="Lan" Width="16" Height="16" Margin="0,0,5,0"/>
                         <TextBlock Text="Local Network"/>
                    </StackPanel>
                </TabItem.Header>
                <ListView x:Name="DiscoveredList" Background="{StaticResource SurfaceBrush}" BorderThickness="0" Foreground="White" SelectionChanged="DiscoveredList_SelectionChanged" Margin="0,10,0,0">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <materialIcons:MaterialIcon Kind="Monitor" Width="24" Height="24" Foreground="#4CAF50" Margin="0,0,10,0"/>
                                <StackPanel Grid.Column="1">
                                    <TextBlock Text="{Binding Name}" FontWeight="Bold"/>
                                    <TextBlock Text="{Binding}" FontSize="12" Opacity="0.7"/>
                                </StackPanel>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </TabItem>
            
            <TabItem>
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal">
                         <materialIcons:MaterialIcon Kind="History" Width="16" Height="16" Margin="0,0,5,0"/>
                         <TextBlock Text="Recent"/>
                    </StackPanel>
                </TabItem.Header>
                <ListView x:Name="RecentList" Background="{StaticResource SurfaceBrush}" BorderThickness="0" Foreground="White" SelectionChanged="RecentList_SelectionChanged" Margin="0,10,0,0">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" Margin="5">
                                <materialIcons:MaterialIcon Kind="History" Width="16" Height="16" Foreground="Gray" Margin="0,0,10,0"/>
                                <TextBlock Text="{Binding}"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </TabItem>
        </TabControl>

        <!-- Cancel -->
        <Button Grid.Row="3" Content="Cancel" Margin="0,15,0,0" Height="30" Click="Cancel_Click" Background="Transparent" Foreground="#888888" BorderThickness="0">
             <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <materialIcons:MaterialIcon Kind="Close" Width="16" Height="16" Margin="0,0,5,0"/>
                            <TextBlock Text="{TemplateBinding Content}"/>
                        </StackPanel>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                             <Setter Property="Foreground" Value="White"/>
                             <Setter Property="Cursor" Value="Hand"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Button.Template>
        </Button>
    </Grid>
</Window>
