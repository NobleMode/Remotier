<Window x:Class="Remotier.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialIcons="clr-namespace:Material.Icons.WPF;assembly=Material.Icons.WPF"
        Title="Remotier" Height="450" Width="600"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundBrush}"
        ResizeMode="CanMinimize">
    
    <Window.Resources>
        <Style x:Key="LargeIconButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="10" Padding="{TemplateBinding Padding}">
                            <StackPanel HorizontalAlignment="Center">
                                <ContentPresenter ContentSource="Tag" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                                <TextBlock Text="{TemplateBinding Content}" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                         <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#444444"/>
                                <Setter Property="Cursor" Value="Hand"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,40,0,20">
             <materialIcons:MaterialIcon Kind="RemoteDesktop" Width="40" Height="40" Foreground="{StaticResource AccentBrush}" Margin="0,0,15,0"/>
             <StackPanel VerticalAlignment="Center">
                 <TextBlock Text="REMOTIER" FontSize="32" FontWeight="Bold" Foreground="{StaticResource AccentBrush}" Effect="{x:Null}"/>
                 <TextBlock Text="Remote Desktop Solution" FontSize="14" Foreground="#888888"/>
             </StackPanel>
        </StackPanel>

        <!-- Main Buttons -->
        <Border Grid.Row="1" Margin="20" VerticalAlignment="Top">
            <WrapPanel HorizontalAlignment="Center">
                <Button Content="Start Hosting" Style="{StaticResource LargeIconButtonStyle}" Width="200" Click="Host_Click">
                    <Button.Tag>
                        <materialIcons:MaterialIcon Kind="ServerNetwork" Width="64" Height="64" Foreground="#4CAF50"/>
                    </Button.Tag>
                </Button>

                <Button Content="Connect to PC" Style="{StaticResource LargeIconButtonStyle}" Width="200" Click="Connect_Click">
                    <Button.Tag>
                        <materialIcons:MaterialIcon Kind="MonitorDashboard" Width="64" Height="64" Foreground="#2196F3"/>
                    </Button.Tag>
                </Button>
            </WrapPanel>
        </Border>
    </Grid>
</Window>
