<Window x:Class="Remotier.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Remotier" Height="350" Width="500"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundBrush}"
        ResizeMode="CanMinimize">
    <Grid>
        <Border BorderBrush="{StaticResource SurfaceBrush}" BorderThickness="1" Margin="10">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="REMOTIER" FontSize="32" FontWeight="Bold" Foreground="{StaticResource AccentBrush}" HorizontalAlignment="Center" Margin="0,0,0,10" Effect="{x:Null}"/>
                <TextBlock Text="Remote Desktop Solution" FontSize="14" Foreground="#888888" HorizontalAlignment="Center" Margin="0,0,0,40"/>
                
                <Button Content="Start Hosting" Width="220" Height="50" FontSize="16" Click="Host_Click" Margin="0,0,0,15"/>
                <Button Content="Connect to PC" Width="220" Height="50" FontSize="16" Click="Connect_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
