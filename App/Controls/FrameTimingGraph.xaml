<UserControl x:Class="Remotier.Controls.FrameTimingGraph"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="300"
             Background="#AA000000">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <StackPanel Orientation="Horizontal" Margin="5">
            <TextBlock Text="Frame Timing" Foreground="White" FontWeight="Bold" FontSize="10"/>
            <TextBlock x:Name="StatsText" Text="0ms" Foreground="#00FF00" Margin="10,0,0,0" FontSize="10"/>
        </StackPanel>

        <!-- Use a Canvas to draw the graph manually using Rectangles or Lines -->
        <Canvas Grid.Row="1" x:Name="GraphCanvas" ClipToBounds="True" Margin="2"/>
    </Grid>
</UserControl>
